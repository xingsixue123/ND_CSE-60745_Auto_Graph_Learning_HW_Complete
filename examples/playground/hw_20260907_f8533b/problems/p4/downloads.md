# Downloads and installs for this problem

One line per item: `- <name> | <how> | <path> | <why>`
If you install nothing, write `- (nothing installed)`.

- (nothing installed)

No venv, no `pip install`, no clone, no download, no `tlmgr install`. Everything needed
was already present: `python3` (conda base 3.12) with `sympy` and `pymupdf` for
`solve.py` and the PDF cropping, and TinyTeX `pdflatex` with `amsmath`, `booktabs`,
`enumitem` and `lmodern`, all of which were already installed.

