# Downloads and installs for this problem

One line per item: `- <name> | <how> | <path> | <why>`
If you install nothing, write `- (nothing installed)`.

- (nothing installed)

Everything this problem needed was already on the machine: `python3` (stdlib
`fractions` only, for `verify.py`), `PIL` (upscaling a crop of `page_001.png`), and
TinyTeX `pdflatex` + `pdftoppm` (test-compiling and proofreading the fragment).
No venv, no `pip`, no downloads.

