# Downloads and installs for this problem

One line per item: `- <name> | <how> | <path> | <why>`
If you install nothing, write `- (nothing installed)`.

- (nothing installed)

Stock `python3` was used for integer arithmetic in `check.py`, and the preinstalled
TinyTeX for the compile check (`booktabs` and `float` were already present). No venv,
no pip, no downloads.

