# Downloads and installs for this problem

One line per item: `- <name> | <how> | <path> | <why>`
If you install nothing, write `- (nothing installed)`.

- (nothing installed)

No venv, no `pip install`, no downloads. Everything used was already on the machine per
`env.md`: `python3` (conda base 3.12) with `sympy` for `verify.py`, `PIL` for cropping the
rendered PDF, and TinyTeX `pdflatex` with `amsmath`/`amssymb`/`geometry` for the
compile-test. Nothing to clean up outside this playground directory.

