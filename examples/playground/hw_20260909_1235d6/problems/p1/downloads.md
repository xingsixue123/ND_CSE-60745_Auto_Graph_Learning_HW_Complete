# Downloads and installs for this problem

One line per item: `- <name> | <how> | <path> | <why>`
If you install nothing, write `- (nothing installed)`.

- (nothing installed)

Prose-only problem: no code, no data, no plotting. Everything used (`pdflatex`, `pdftoppm`,
`pdfinfo`, `python3`) was already on the machine per `framework/env.md`. Nothing was fetched
from the network and nothing was written outside PLAYGROUND/OUTPUT.

