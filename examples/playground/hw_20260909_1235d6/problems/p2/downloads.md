# Downloads and installs for this problem

One line per item: `- <name> | <how> | <path> | <why>`
If you install nothing, write `- (nothing installed)`.

- (nothing installed)

This problem is plain prose (Ex. 1.5–1.8, OS concepts). No code was run, no packages
were fetched, no venv was created. The only tooling used was the already-present
`pdflatex` (TinyTeX, per env.md) to smoke-test that the fragment compiles.
