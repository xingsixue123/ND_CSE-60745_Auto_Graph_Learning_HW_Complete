# Downloads and installs for this problem

One line per item: `- <name> | <how> | <path> | <why>`
If you install nothing, write `- (nothing installed)`.

- (nothing installed)

Nothing was downloaded and no package was installed. The problem needed only
networkx and matplotlib, both already present in the pre-existing interpreter
`/home/xing/miniconda3/envs/py311/bin/python` (networkx 3.3, matplotlib 3.10.8).
Base `python3` (networkx 3.6.1) was used as a second, independent check. No venv
was created.

Playground-only scratch (not an install): `.mplconfig/`, a matplotlib cache dir
needed because `~/.config/matplotlib` is read-only; set via `MPLCONFIGDIR`.

