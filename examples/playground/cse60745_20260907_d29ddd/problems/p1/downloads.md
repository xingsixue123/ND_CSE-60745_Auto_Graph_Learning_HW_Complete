# Downloads and installs for this problem

One line per item: `- <name> | <how> | <path> | <why>`
If you install nothing, write `- (nothing installed)`.

- (nothing installed)

Nothing was downloaded or pip-installed. Everything needed was already present:
base `python3` (numpy, PIL, networkx) for the figure transcription, and
`/home/xing/miniconda3/envs/py311/bin/python` (networkx 3.3 + matplotlib 3.10.8)
for the graph computation and the figures.

Scratch directories created inside the PLAYGROUND (caches//outputs, not installs):
- `.mplcfg/`  | `MPLCONFIGDIR` for matplotlib (`~/.config/matplotlib` is read-only)
- `work/tex/` | local pdflatex test-compile of the answer fragment
- `figs/`     | PNG previews of the figures, for my own visual inspection
