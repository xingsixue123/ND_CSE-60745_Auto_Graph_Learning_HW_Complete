# Downloads and installs for this problem

One line per item: `- <name> | <how> | <path> | <why>`
If you install nothing, write `- (nothing installed)`.

- (nothing installed) | only the pre-existing system gcc 12.2.0 at /usr/bin/gcc was used | n/a | compiling forkexec.c; no venv, no pip, no downloads were needed

