# Downloads and installs for this problem

One line per item: `- <name> | <how> | <path> | <why>`
If you install nothing, write `- (nothing installed)`.

- (nothing installed)

This problem is a plain prose answer. Only pre-installed tools were used:
`pdflatex`, `pdftoppm`, `pdfinfo` and the system `python3`, all for checking the
deliverable rather than producing it.

