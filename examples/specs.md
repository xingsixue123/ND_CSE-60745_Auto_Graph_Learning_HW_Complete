inputdir: `input/`

outputdir: `output/`

<!--
Paths are relative to the repository root; the master is also given them as
absolute paths in its runtime facts, so this file does not have to repeat them.

Anything else worth telling the master goes here: a submission format the course
requires, a preferred library, a question to skip, a deadline. It can stay this
short -- see examples/specs.md, which is the entire specification the HW1 run was
given.
-->

we are working on hw2 partb, instructions here: `/home/xing/project/auto_hw_complete/input/CSE60745-Hands-on_HW2-Part(B)-1.pdf`, and hw2 paet b starts from page 7 "HW2-Part(B): 30 pts" ignore prev slides's solutions. I think it is the instruction where real hw guideline and problem definitions are in the juptter notebook: `/home/xing/project/auto_hw_complete/input/graph_embedding-1.ipynb`

